# Dockerfile

FROM nginx:alpine

# Copy the NGINX configuration file
COPY nginx.conf /etc/nginx/nginx.conf

# Copy static files (optional, if you want to include them during build)
# COPY /path/to/local/staticfiles /path/to/staticfiles/

# Expose port 80
EXPOSE 80
